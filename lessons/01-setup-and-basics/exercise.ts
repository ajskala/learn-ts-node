// Lesson 1 exercise
// Fill in each TODO. Run `npm run check` from the project root to verify types,
// then `node lessons/01-setup-and-basics/exercise.ts` to run it.

// TODO 1: declare a constant `city` typed as a string, set to any city name.

// TODO 2: declare a constant `population` typed as a number.

// TODO 3: declare a constant `landmarks` that is an array of strings with at least
// two landmark names in `city`.

// TODO 4: write a function `describeCity` that takes (city: string, population: number)
// and returns a string like "Chicago has 2700000 people."
// Give it an explicit return type of `string`.

// TODO 5: write a function `summarize` that takes `landmarks: string[]` and returns
// a single string joining them with ", ". Give it an explicit return type.

// TODO 6: call both functions and console.log() the results.

// TODO 7 (unknown/narrowing practice): write a function `safeLength` that takes a
// parameter typed `unknown` and returns a number: the string's length if the input
// is a string, otherwise 0. Do not use `any`.
